$(window).on('page:update page:load', function() {

  $('.main_image_preview input:file').uploadThumbs({
  });
  $('.sub_image_preview1 input:file').uploadThumbs({
  });
  $('.sub_image_preview2 input:file').uploadThumbs({
  });
  $('.user_avatar_preview input:file').uploadThumbs({
  });

});
